version: 2

models:
  - name: metricflow_time_spine
    description: "Daily calendar table that MetricFlow joins against for time-grain metric queries."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
