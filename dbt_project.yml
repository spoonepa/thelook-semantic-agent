name: 'thelook_semantic'
version: '1.0.0'
config-version: 2

profile: 'thelook_semantic'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  thelook_semantic:
    staging:
      +materialized: view
    marts:
      +materialized: table
